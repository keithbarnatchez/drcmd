% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/drcmd.R
\name{est_psi_tml}
\alias{est_psi_tml}
\title{Estimate causal effects through targeted maximum likelihood}
\usage{
est_psi_tml(
  idx,
  Y,
  A,
  X,
  R,
  Z,
  m_1_hat,
  m_0_hat,
  g_hat,
  kappa_hat,
  phi_1_hat,
  phi_0_hat,
  varphi_hat,
  att = FALSE,
  atc = FALSE
)
}
\arguments{
\item{idx}{Indices of the training set}

\item{Y}{Outcome vector}

\item{A}{Treatment vector}

\item{X}{Dataframe of covariates}

\item{R}{Complete case indicator vector}

\item{Z}{Dataframe containing all variables that are never subject to missingness}

\item{m_1_hat}{A numeric vector containing the fitted outcome predictions under A=1}

\item{m_0_hat}{A numeric vector containing the fitted outcome predictions under A=0}

\item{g_hat}{A numeric vector containing the fitted treatment propensity scores}

\item{kappa_hat}{A numeric vector containing the fitted values of kappa}

\item{phi_1_hat}{Estimates of EIC for \code{E[Y(1)]}}

\item{phi_0_hat}{Estimates of \code{E[Y(0)]}}

\item{varphi_hat}{A list containing estimate of \code{E[phi_a | Z]}}

\item{att}{Logical. Not supported under TML; must be FALSE}

\item{atc}{Logical. Not supported under TML; must be FALSE}
}
\value{
A list with point estimates, variances, and influence-curve
contributions for E\link{Y(1)}, E\link{Y(0)}, and ATE
}
\description{
Function for obtaining estimates of causal estimands through
targeted maximum likelihood. To be called within drcmd_est_fold(). Runs a
separate TML fluctuation for each of the ATE, E\link{Y(1)} and E\link{Y(0)}, so each
estimand is debiased directly. ATT/ATC are not supported here -- use the
one-step option (tml=FALSE) for those.
}
\examples{
\dontrun{
# Typically called internally by drcmd_est_fold() when tml = TRUE.
# Requires nuisance estimates from get_nuisance_ests() and
# pseudo-outcome estimates from est_varphi_main().
}
}
\keyword{internal}
